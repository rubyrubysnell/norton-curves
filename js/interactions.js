// function triggers every time the user scrolls

function mouseWheel(event) {
    // delta means increment
    // if the user is scrolling quickly, it will be higher, and vice versa
    const deltaWheel = event.delta * 0.05
    //update offset
    // test3 = b + deltaWheel
    // console.log(b)

    // b = b + deltaWheel
}